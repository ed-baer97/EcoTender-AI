[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ecotender-shared"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["pydantic>=2.6", "httpx>=0.27"]

[tool.setuptools.packages.find]
where = ["."]
include = ["ecotender_shared*"]
